name: Sync Upstream

on:
  workflow_dispatch:
    # 仅手动触发，避免定时任务自动改写 main 历史
    inputs:
      dry_run:
        description: 'Dry run mode (只检查不推送)'
        required: false
        default: 'true'
        type: boolean

env:
  UPSTREAM_REPO: 'Wei-Shaw/claude-relay-service'
  UPSTREAM_BRANCH: 'main'

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.UPSTREAM_BRANCH }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/${{ env.UPSTREAM_REPO }}.git || \
          git remote set-url upstream https://github.com/${{ env.UPSTREAM_REPO }}.git

      - name: Fetch upstream
        run: git fetch upstream ${{ env.UPSTREAM_BRANCH }}

      - name: Check for updates
        id: check
        run: |
          BEHIND=$(git rev-list --count HEAD..upstream/${{ env.UPSTREAM_BRANCH }})
          echo "behind=$BEHIND" >> $GITHUB_OUTPUT

          if [ "$BEHIND" -eq 0 ]; then
            echo "status=up-to-date" >> $GITHUB_OUTPUT
            echo "::notice::Already up to date with upstream"
          else
            echo "status=needs-sync" >> $GITHUB_OUTPUT
            echo "::notice::Behind upstream by $BEHIND commits"

            # 获取新提交信息
            echo "## New commits from upstream:" >> $GITHUB_STEP_SUMMARY
            git log --oneline HEAD..upstream/${{ env.UPSTREAM_BRANCH }} >> $GITHUB_STEP_SUMMARY
          fi

      - name: Attempt rebase
        id: rebase
        if: steps.check.outputs.status == 'needs-sync'
        run: |
          echo "Attempting rebase onto upstream/${{ env.UPSTREAM_BRANCH }}..."

          if git rebase upstream/${{ env.UPSTREAM_BRANCH }}; then
            echo "result=success" >> $GITHUB_OUTPUT
            echo "::notice::Rebase successful"
          else
            echo "result=conflict" >> $GITHUB_OUTPUT
            echo "::error::Rebase failed due to conflicts"
            git rebase --abort
            exit 1
          fi
        continue-on-error: true

      - name: Setup Node.js
        if: steps.rebase.outputs.result == 'success'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        if: steps.rebase.outputs.result == 'success'
        run: npm ci

      - name: Install web dependencies
        if: steps.rebase.outputs.result == 'success'
        run: cd web/admin-spa && npm ci

      - name: Build web interface
        id: build
        if: steps.rebase.outputs.result == 'success'
        run: |
          if npm run build:web; then
            echo "result=success" >> $GITHUB_OUTPUT
            echo "::notice::Build successful"
          else
            echo "result=failed" >> $GITHUB_OUTPUT
            echo "::error::Build failed"
            exit 1
          fi
        continue-on-error: true

      - name: Verify brand files
        id: verify
        if: steps.build.outputs.result == 'success'
        run: |
          echo "Verifying brand customization files..."

          ERRORS=0

          # 检查 logo
          if [ ! -f "web/admin-spa/public/logo.png" ] && [ ! -f "web/admin-spa/public/logo.svg" ]; then
            echo "::error::Missing logo (expected web/admin-spa/public/logo.png or logo.svg)"
            ERRORS=$((ERRORS + 1))
          fi

          # 检查 favicon / touch icons（兼容 favicon.ico 或 PNG 组合）
          if [ ! -f "web/admin-spa/public/favicon.ico" ]; then
            MISSING_ICONS=0
            for ICON_FILE in favicon-16x16.png favicon-32x32.png apple-touch-icon.png; do
              if [ ! -f "web/admin-spa/public/${ICON_FILE}" ]; then
                echo "::error::Missing ${ICON_FILE}"
                MISSING_ICONS=$((MISSING_ICONS + 1))
              fi
            done
            if [ $MISSING_ICONS -gt 0 ]; then
              ERRORS=$((ERRORS + 1))
            fi
          fi

          # 检查品牌名称
          if ! grep -q "Whoos Solutions" web/admin-spa/index.html; then
            echo "::error::Brand name missing in index.html"
            ERRORS=$((ERRORS + 1))
          fi

          # 检查配色（实际路径含 assets/ 层级）
          if ! grep -q "#b31b1b\|#e4002b\|#ffcd00" web/admin-spa/src/assets/styles/global.css; then
            echo "::warning::Brand colors may have been modified in global.css"
          fi

          if [ $ERRORS -gt 0 ]; then
            echo "result=failed" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "result=success" >> $GITHUB_OUTPUT
            echo "::notice::Brand verification passed"
          fi
        continue-on-error: true

      - name: Get upstream version
        id: version
        if: steps.verify.outputs.result == 'success'
        run: |
          # 优先读取 VERSION 文件（不依赖 tag；fork 上 tag 可能被污染）
          RAW_VERSION=$(cat VERSION 2>/dev/null | tr -d ' \n\r' || echo "")
          if [ -n "$RAW_VERSION" ]; then
            VERSION="v$RAW_VERSION"
          else
            VERSION=$(git describe --tags upstream/${{ env.UPSTREAM_BRANCH }} --abbrev=0 2>/dev/null || echo "unknown")
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "::notice::Upstream version: $VERSION"

      - name: Update sync record
        if: steps.verify.outputs.result == 'success'
        run: |
          DATE=$(date +%Y-%m-%d)
          VERSION="${{ steps.version.outputs.version }}"
          COMMITS="${{ steps.check.outputs.behind }}"

          # 在 "下次更新请在此添加记录" 之前插入新记录
          sed -i "/\*\*下次更新请在此添加记录\*\*/i | $DATE | $VERSION | 同步 $COMMITS 个提交 (自动) | 无冲突 | ✅ 成功 | GitHub Actions 自动同步 |" SYNC_UPSTREAM.md

          git add SYNC_UPSTREAM.md
          git commit -m "docs: 更新同步记录 - $VERSION (自动同步)"

      - name: Push changes
        id: push
        if: steps.verify.outputs.result == 'success' && github.event.inputs.dry_run != 'true'
        run: |
          echo "Pushing changes to origin..."
          # 先 fetch origin 再处理 CI 自动提交（sync VERSION），避免 --force-with-lease stale 失败
          git fetch origin ${{ env.UPSTREAM_BRANCH }}
          AHEAD=$(git rev-list --count origin/${{ env.UPSTREAM_BRANCH }}..HEAD)
          BEHIND_ORIGIN=$(git rev-list --count HEAD..origin/${{ env.UPSTREAM_BRANCH }})
          echo "Ahead of origin: $AHEAD, Behind origin: $BEHIND_ORIGIN"
          if [ "$BEHIND_ORIGIN" -gt 0 ]; then
            echo "::notice::origin has $BEHIND_ORIGIN commits not in local (likely CI auto-commits), rebasing..."
            git rebase origin/${{ env.UPSTREAM_BRANCH }}
          fi
          git push origin ${{ env.UPSTREAM_BRANCH }} --force-with-lease
          echo "::notice::Successfully pushed to origin"

      - name: Dry run notice
        if: steps.verify.outputs.result == 'success' && github.event.inputs.dry_run == 'true'
        run: |
          echo "::warning::Dry run mode - changes were NOT pushed"
          echo "## Dry Run Summary" >> $GITHUB_STEP_SUMMARY
          echo "Rebase and build successful, but changes were not pushed (dry run mode)" >> $GITHUB_STEP_SUMMARY

      - name: Create issue on failure
        if: always() && steps.check.outputs.status == 'needs-sync' && (steps.rebase.outputs.result == 'conflict' || steps.build.outputs.result == 'failed' || steps.verify.outputs.result == 'failed' || steps.push.outcome == 'failure')
        uses: actions/github-script@v7
        with:
          script: |
            const rebaseResult = '${{ steps.rebase.outputs.result }}';
            const buildResult = '${{ steps.build.outputs.result }}';
            const verifyResult = '${{ steps.verify.outputs.result }}';
            const behind = '${{ steps.check.outputs.behind }}';

            let title, body;

            if (rebaseResult === 'conflict') {
              title = '🔴 上游同步失败: Rebase 冲突';
              body = `## 同步失败通知\n\n上游有 ${behind} 个新提交，但自动 rebase 遇到冲突。\n\n### 需要手动处理\n\n请在本地执行以下命令：\n\n\`\`\`bash\ncd /Users/jeffreyhu/DEV/claude-relay-service\ngit fetch upstream\ngit rebase upstream/main\n# 解决冲突后\ngit add .\ngit rebase --continue\nnpm run build:web\ngit push origin main --force-with-lease\n\`\`\`\n\n### 冲突处理策略（Rebase 语境）\n- 功能代码 → 保留上游版本（rebase 时 \`--ours\` = 上游；更安全是用 \`git show upstream/main:<path> > <path>\`）\n- UI/样式文件 → 保留我们的版本（rebase 时 \`--theirs\` = 我们；例如 \`git checkout --theirs -- <path>\`）`;
            } else if (buildResult === 'failed') {
              title = '🔴 上游同步失败: 构建错误';
              body = `## 同步失败通知\n\nRebase 成功，但 \`npm run build:web\` 构建失败。\n\n请检查构建日志并手动修复。`;
            } else if (verifyResult === 'failed') {
              title = '🔴 上游同步失败: 品牌文件验证失败';
              body = `## 同步失败通知\n\nRebase 和构建成功，但品牌定制文件验证失败。\n\n可能是上游更新覆盖了我们的定制内容，请手动检查：\n- \`web/admin-spa/public/logo.png\` (或 \`logo.svg\`)\n- \`web/admin-spa/public/favicon-32x32.png\`\n- \`web/admin-spa/public/favicon-16x16.png\`\n- \`web/admin-spa/public/apple-touch-icon.png\`\n- \`web/admin-spa/index.html\` (品牌名称)\n- \`web/admin-spa/src/assets/styles/global.css\` (配色)`;
            } else if ('${{ steps.push.outcome }}' === 'failure') {
              title = '🔴 上游同步失败: 推送失败';
              body = `## 同步失败通知\n\nRebase/构建/验证都通过了，但推送到 \`origin/main\` 失败。\n\n请检查 Actions 日志中的 push 步骤，通常是远端分支再次变化或权限问题。`;
            } else {
              title = '🔴 上游同步失败: 未知错误';
              body = `## 同步失败通知\n\n请检查 GitHub Actions 日志了解详情。`;
            }

            // 检查是否已有未关闭的同步失败 issue
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'sync-failure'
            });

            if (issues.data.length === 0) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: title,
                body: body,
                labels: ['sync-failure']
              });
            } else {
              // 更新现有 issue
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issues.data[0].number,
                body: `## 新的同步尝试失败\n\n${body}\n\n---\n时间: ${new Date().toISOString()}`
              });
            }

      - name: Summary
        if: always()
        run: |
          echo "## Sync Summary" >> $GITHUB_STEP_SUMMARY
          echo "| Step | Result |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Check updates | ${{ steps.check.outputs.status }} (${{ steps.check.outputs.behind }} commits behind) |" >> $GITHUB_STEP_SUMMARY
          echo "| Rebase | ${{ steps.rebase.outputs.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build | ${{ steps.build.outputs.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Verify | ${{ steps.verify.outputs.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
